// Global Object

// console.log(global);


/* global.setTimeout(() => {
   console.log('in the time out'); 
   // stops int from running indefinitely -- runs once a second until setTimeout starts at 3 seconds
   clearInterval(int);
}, 3000);

const int = setInterval(() => {
    console.log('in the interval');
}, 1000) ;*/

// get directory structure
console.log(__dirname);
// get directory with current file 
console.log(__filename);

